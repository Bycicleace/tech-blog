<article class="post">
    <div class="card-title">
        <h3>{{title}}</h3>
        <span>Posted by {{user.username}} on {{created_at}}</span>
    </div>
    <div class="card-body">
        <span>{{body}}</span>
    </div>
</article>